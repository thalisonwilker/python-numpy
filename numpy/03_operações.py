import numpy as np

arr = np.arange(0, 16)

# operações, são realizadas indice a indice
arr + arr, arr - arr, arr / arr, arr * arr, arr ** 2, arr+100

# raiz de todos os elementos do array
np.sqrt(arr)

#media
np.mean(arr)

#máximo
np.max(arr)
arr.max()
